# 🚀 UniEmail Agent — 全维度优化与新增功能计划

## 📊 当前项目概览

| 维度 | 状态 |
|------|------|
| Agent 引擎 | HermesOrchestrator / GraphAgent / ClaudeAgent / Playwright 回退 |
| 智能路由 | 三路意图分类 (new_crawl / incremental / simple_query) |
| 知识系统 | Mem0 + 文件 Skills + 全局经验 + 通用经验 + 断点续传 |
| 前端 UI | Next.js 16 + shadcn/ui v4 + WebSocket 实时流 |
| 数据导出 | 6 格式 (CSV/XLSX/MD/HTML/PDF/DOCX) |
| 邮件发送 | SMTP 验证 + 模板 + 批量发送 + 状态追踪 |
| 高校库 | 中国高校 2921 所数据库 + 省市筛选 + 文件管理 |
| 质量门 | 自动验证 + 脏数据过滤 + 去重 |

---

## 🔴 Phase A: 现有功能深度优化（零新功能，纯打磨）

### A1. 后端性能与稳定性

| # | 问题 | 方案 | 难度 |
|---|------|------|------|
| 1 | `main.py` 2393 行，WS handler 极其庞大 | 拆分 WebSocket handler 到独立模块 `ws_handler.py`，按职责分：`connection_manager.py`（WS 生命周期）、`message_filter.py`（消息过滤逻辑）、`progress_pump.py`（进度泵） | 中 |
| 2 | `_validate_crawl_output()` 在 WS 循环中阻塞 | 移到 `done` 事件后异步执行，WS 先 close 再跑校验 | 低 |
| 3 | `progress_pump_llm` 每 30 秒调一次 LLM API | 浪费 token。改为：只在 `stage`/`stats` 更改时触发，去掉轮询 | 低 |
| 4 | 技能文件最大 30 个，迅速淘汰历史 | 改为按 `uni_name + 最新月份` 聚合，按月归档旧文件 | 低 |
| 5 | `_update_universal_tips()` 300 行后粗暴压缩 | 改用 LLM 进行有损压缩（保留核心策略数，定期合并同类项） | 中 |
| 6 | Mem0 双写 + Skills 双写 + 文件系统三写一致性问题 | 引入统一的 `KnowledgeStore` 抽象，写时保证三路一致，失败时重试队列 | 中 |

### A2. 前端响应与渲染优化

| # | 问题 | 方案 | 难度 |
|---|------|------|------|
| 7 | ChatArea.tsx 469 行，27 个 `useXxxStore` selector | 合并成 `useChatAreaContext` 或拆分大型组件 | 中 |
| 8 | 高校库 `flatUniversities` 频繁 re-compute | 用 `useDeferredValue` + 虚拟列表 (tanstack-virtual) 替代 slice 截断 | 中 |
| 9 | 邮件面板 body 模板硬编码了 "想 and 您" 拼写错误 | 修复模板默认值 | 低 |
| 10 | WebSocket 重连没有指数退避 | 实现 `exponentialBackoff(min=1s, max=30s)` 加上 jitter | 低 |
| 11 | 每次 `activeProcs` 变化都 setInterval 每 3 秒轮询 | 改为 WebSocket 主动推送状态变更，或只在用户打开对话框时轮询 | 中 |

### A3. Agent 执行优化

| # | 问题 | 方案 | 难度 |
|---|------|------|------|
| 12 | `claude_agent.py` 子进程用 3600 秒硬超时 | 改为动态超时：基于 `学院数 * 120秒 + 基础 600秒` 估算 | 低 |
| 13 | `MAX_STEPS = 2000` 对大数据集不够 | 改为 `大学教师数 / 10` 动态步数 | 低 |
| 14 | 输入/输出安全检测 `check_input/output` 在 `main.py` 线内 | 独立成 FastAPI middleware，所有端点共用 | 低 |
| 15 | Agent 日志 `agent_output.log` 逐行 append 无轮转 | 按 10MB 轮转，保留最近 3 个归档文件 | 低 |

---

## 🟠 Phase B: 用户体验优化

### B1. 交互体验

| # | 功能 | 描述 | 难度 |
|---|------|------|------|
| 16 | **流式 Token 实时显示** | 当前 text 消息攒完 buffer 才显示。改为：WebSocket 逐 token 推送，前端打字机效果 | 中 |
| 17 | **任务结果对比** | 增量任务完成后，自动显示 "v1 有 500 人 → v2 有 650 人，增长 30%"，带差异高亮 | 中 |
| 18 | **搜索增强** | 全平台搜索（历史任务 + 高校库 + 教师姓名 + 邮箱），一键跳转 | 低 |
| 19 | **一键清洗 & 去重** | 当前高校库表格 CRUD 入口深。加一个 "一键清洗" 大按钮 | 低 |
| 20 | **批量操作行** | 表格支持多选 + 批量删除 / 批量导出选中行 | 低 |
| 21 | **重试失败学院** | 爬取失败的学院列表持久化，支持"只重试失败的 X 个学院" | 中 |
| 22 | **右键菜单** | 消息气泡右键菜单 (复制全部 / 复制邮箱 / 下载文件) | 低 |
| 23 | **WebSocket 连接条** | 当前断了没明显提示。加一个 `ConnectionBanner` | 低 |
| 24 | **导出进度条** | 大数据量导出显示进度百分比，而不是 `exporting: xlsx` 字样 | 中 |
| 25 | **文件预览** | 在高校库直接预览 CSV 前 20 行（表结构），不用下载后才能看 | 低 |
| 26 | **全屏模式** | 高校库 / 邮件发送面板支持全屏展开，适配宽屏显示器 | 低 |

### B2. 视觉与品牌

| # | 功能 | 描述 | 难度 |
|---|------|------|------|
| 27 | **暗色模式优化** | 当前部分 border/muted 在暗色模式下对比度不足 | 低 |
| 28 | **加载骨架屏** | 页面初始加载时显示 Skeleton 而不是白屏 | 低 |
| 29 | **多语言支持** | i18n 基础架构 (zh-CN / en)，方便出海推广 | 中 |
| 30 | **Logo 动画** | 当前是静态 PNG Logo，换成带动画的 SVG | 低 |
| 31 | **统计仪表板** | 首页展示总教师数、总邮箱数、高校覆盖率等看板数据 | 中 |

---

## 🟡 Phase C: 新增核心功能

### C1. Agent 能力增强

| # | 功能 | 描述 | 难度 | 优先级 |
|---|------|------|------|--------|
| 32 | **PDF/图片 OCR 邮箱提取** | 高校官网放的 PDF 教师名录 / 截图 → OCR 提取邮箱。集成 `marker-pdf` 或 `pymupdf` | 高 | P0 |
| 33 | **多 Agent 并行爬取** | 当爬取多学院时，每个学院分配独立子 Agent 并行执行，总耗时大幅降低 | 高 | P0 |
| 34 | **爬取中人工干预** | Agent 卡住时允许用户手动输入指令（"跳过这个学院""换个 URL 模式"） | 高 | P1 |
| 35 | **邮箱验证** | 提取后自动发验证邮件或 MX 记录查询，排除无效邮箱 | 中 | P1 |
| 36 | **智能频率控制** | 自动检测反爬信号，动态调整请求间隔（支持 429/503 自动降速） | 中 | P1 |
| 37 | **定时爬取** | 配置 cron 任务：每周/每月自动抓取某高校更新的教师数据 | 中 | P2 |
| 38 | **爬取策略模板** | 按高校类型（985/211/普通/专科/海外）预配置爬取策略，新任务自动匹配 | 低 | P2 |
| 39 | **数据结构推断** | 自动识别页面表格结构、字段映射、生成规范化 schema | 中 | P2 |

### C2. 邮件系统增强

| # | 功能 | 描述 | 难度 | 优先级 |
|---|------|------|------|--------|
| 40 | **邮件模板库** | 预设多个模板（邀请函 / 投稿 / 招生 / 合作），支持保存自定义模板 | 中 | P0 |
| 41 | **发送计划（定时发）** | 指定未来时间发送邮件组 | 中 | P1 |
| 42 | **发送限速** | 自定义每分钟/每小时最大发送量，防止被 SMTP 封禁 | 低 | P1 |
| 43 | **退信反馈** | 自动检测退信邮箱，标记为无效并在数据中去除 | 高 | P2 |
| 44 | **邮件发送历史** | 持久化发送记录到数据库，可跨 session 查看 | 中 | P1 |
| 45 | **HTML 邮件模板** | 支持富文本编辑器和 HTML 邮件正文 | 中 | P2 |
| 46 | **变量高亮/补全** | 模板编辑器中 `{{姓名}}` 等变量高亮，支持自动补全 | 低 | P2 |

### C3. 数据管理与协作

| # | 功能 | 描述 | 难度 | 优先级 |
|---|------|------|------|--------|
| 47 | **数据版本对比** | 同高校多次爬取结果的 diff 视图（新增/删除/更改的行） | 中 | P0 |
| 48 | **数据导出为 CSV+XLSX 定制** | 支持选择导出哪些列，自定义导出文件名 | 低 | P0 |
| 49 | **教师详情页** | 点击教师名称展开个人资料页（含主页URL、研究方向、论文列表等） | 中 | P1 |
| 50 | **批量更新** | 通过上传 CSV/XLSX 批量更新教师数据 | 低 | P1 |
| 51 | **团队协作** | 多用户共享高校库，分工爬取不同学院，合并结果 | 高 | P2 |
| 52 | **数据标签** | 为教师打标签（已联系、已回复、博导、教授等），支持筛选 | 中 | P1 |
| 53 | **数据导出 API** | 暴露 REST API 允许外部系统通过 token 拉取数据 | 低 | P2 |
| 54 | **历史版本快照** | 每次爬取生成快照，可回滚到历史版本 | 中 | P2 |

### C4. 高校库增强

| # | 功能 | 描述 | 难度 | 优先级 |
|---|------|------|------|--------|
| 55 | **高校覆盖热力图** | 地图可视化已覆盖 vs 未覆盖的高校，省份维度 | 中 | P1 |
| 56 | **爬取难度评级** | 自动给高校打难度分（基于反爬强度、页面结构复杂度） | 中 | P1 |
| 57 | **海外高校支持** | 添加海外高校数据库，支持英文网站爬取 | 中 | P2 |
| 58 | **高校别名智能匹配** | "NJAU" → "南京农业大学"，"SEU" → "东南大学"等 | 低 | P0 |

### C5. 系统管理

| # | 功能 | 描述 | 难度 | 优先级 |
|---|------|------|------|--------|
| 59 | **系统日志面板** | 后端运行日志实时查看，错误告警 | 中 | P1 |
| 60 | **Token 用量统计** | LLM API token 消耗统计 + 预算告警（当前只设了 $20 上限） | 中 | P1 |
| 61 | **基于角色的访问控制** | 管理员 / 操作员 / 只读用户三种角色 | 高 | P2 |
| 62 | **One-Click Docker 部署** | `docker-compose up` 一键部署全部服务，减少环境配置 | 中 | P0 |
| 63 | **健康监控面板** | API 响应时间、WebSocket 连接数、Agent 活跃数 Grafana 看板 | 高 | P2 |
| 64 | **自动更新检查** | 检查 GitHub Release 版本，一键更新 | 中 | P2 |

---

## 🟢 Phase D: 架构升级

| # | 变更 | 描述 | 难度 | 收益 |
|---|------|------|------|------|
| 65 | **SQLite 替代 JSON 持久化** | 当前 `data/index.json` + `{task_id}.json` 在 1000+ 任务后读写性能急剧下降。改为 SQLite 支持复杂查询、分页、搜索 | 中 | 高 |
| 66 | **任务队列** | 当前单次 WS handler 全量占线。改为 Celery / RQ 任务队列，支持排队和优先级 | 高 | 高 |
| 67 | **前端状态管理重构** | 多个 zustand store 职责重叠（chat-store / task-store / ui-store）。合并成领域驱动的 store 结构 | 中 | 中 |
| 68 | **API 版本化** | 当前无版本前缀。改为 `/api/v1/...` 为向后兼容做准备 | 低 | 中 |
| 69 | **OpenAPI 文档** | 当前 FastAPI 自动生成但没定制。加 description、example、response model | 低 | 中 |
| 70 | **单元测试覆盖率** | 当前只有 3 个测试文件。为 claude_agent / exporter / cleaner / mailer 加 pytest 覆盖 | 中 | 高 |
| 71 | **E2E 测试** | Playwright E2E 测试核心流程：聊天 → 意图识别 → 爬取完成 → 导出 | 中 | 高 |

---

## 📋 推荐执行优先级

### 立即执行（P0 — 本周）
| 优先级 | 编号 | 原因 |
|--------|------|------|
| 🥇 | 65 | SQLite 替代 JSON — 当前存储架构是瓶颈 |
| 🥇 | 32 | PDF/OCR 邮箱提取 — 大量高校数据在 PDF 里 |
| 🥇 | 33 | 多 Agent 并行爬取 — 爬 30 个学院=30x 提速 |
| 🥇 | 16 | 流式 Token 实时显示 — 用户感知最明显的改善 |
| 🥇 | 12 | 动态超时 — 防止大数据集中途被截断 |

### 短期（P1 — 两周内）
| 编号 | 原因 |
|------|------|
| 1 | WS handler 拆分 — 降低维护成本 |
| 34 | 爬取中人工干预 — 兜底能力，防止空跑 |
| 40 | 邮件模板库 — 高频使用功能 |
| 47 | 数据版本对比 — 增量任务的直接产出 |
| 58 | 高校别名匹配 — 用户体验关键点 |
| 62 | Docker 一键部署 — 降低部署门槛 |

### 中期（P2 — 一个月）
| 编号 | 原因 |
|------|------|
| 70-71 | 单元+E2E 测试 — 防回归 |
| 37 | 定时爬取 — 持续数据更新 |
| 51 | 团队协作 — 多人分工 |
| 59 | 系统日志面板 — 运维刚需 |
| 60 | Token 用量统计 — 成本控制 |
