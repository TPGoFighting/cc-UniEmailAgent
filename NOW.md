# 高校库、邮件发送与任务进度展示改造计划

## Summary

在首页右上角新增 高校库 和 邮件发送 两个入口。
高校库内置全国普通本科院校名录，按省市展示，并支持 985、211、双一流、普通本科 标签筛选；对话采集产出的 CSV/XLSX 自动归档到对应高校，支持前端表格预览。
邮件发送移植 D:\Work\CodexTeac 的 SMTP 验证、预览、发送任务和日志能力到当前 FastAPI 后端。
同时调整对话采集体验：Agent 运行过程不再把全部内部日志展示给前端，前端只显示模拟任务进度，任务结束后只保留完成前最后一句摘要和结果文件。

## Key Changes

- 高校名录数据层：
  - 保存全国普通本科院校基础字段：name、province、city、type、is_985、is_211、is_double_first_class、website/domain。
  - 分类标签非互斥，页面筛选支持 985、211、双一流、普通本科。
  - 普通本科表示未命中 985/211/双一流重点标签的本科院校展示分组。
- 高校库 API：
  - GET /api/universities?province=&tier=&q=：返回省市分组高校列表和抓取数据统计。
  - GET /api/universities/{name}/records：返回该校关联的历史任务、输出文件、表格行数、有效邮箱数。
  - GET /api/universities/{name}/table?task_id=&file=：解析 CSV/XLSX 并返回 {columns, rows, total}。
- 对话采集展示改造：
  - 后端继续记录完整 Agent 内部日志到服务端日志或调试字段，但 WebSocket 不再逐条推送给前端消息流。
  - WebSocket 改为推送少量用户友好的进度事件，例如：正在识别高校、正在访问官网、正在提取教师信息、正在清洗数据、正在生成文件。
  - 历史消息只持久化用户输入、任务完成摘要、下载文件消息和错误消息，不再持久化大量运行日志。
  - 任务完成时前端只展示 Agent 最后一句完成摘要，以及产出的 CSV/XLSX/HTML/PDF/DOCX/MD 下载卡片。
- 前端高校库：
  - 右上角 高校库 按钮打开全屏/侧滑工作区。
  - 顶部提供搜索、地区筛选、标签筛选。
  - 左侧省市和高校列表，右侧展示学校概况、标签、已抓取表格、下载来源。
  - 表格支持前端分页、搜索、按院系过滤、仅看有效邮箱。
- 邮件发送：
  - 右上角 邮件发送 按钮打开发送向导。
  - 选择范围支持学校/院系/教师三级选择。
  - 支持自定义主题和正文，变量包括 {{学校}}、{{姓名}}、{{邮箱}}、{{学院}}、{{职称}}。
  - SMTP 密码只保存在后端运行期内存，不落盘、不进历史消息、不返回前端。
  - 发送前必须完成 SMTP 验证、预览确认；超过 50 封默认要求二次确认。

## Test Plan

- 后端测试：
  - 高校名录可按省市、985、211、双一流、普通本科筛选。
  - 历史 CSV/XLSX 能正确归档到南京大学、南京邮电大学、南京理工大学等已有输出学校。
  - 表格解析兼容中文表头、UTF-8 BOM、空邮箱、非法邮箱。
  - WebSocket 不再向前端推送内部 Agent 原始日志，只推送模拟进度、完成摘要、下载文件和错误。
  - 历史任务消息不再出现大量 log 消息。
  - 邮件预览、SMTP 验证、发送确认、大批量二次确认均按规则拦截。
- 前端测试：
  - cd frontend && npx tsc --noEmit 通过。
  - 对话任务运行中显示简洁进度，不刷屏。
  - 任务完成后只看到最终摘要和结果文件下载卡片。
  - 高校库筛选、学校切换、表格预览、院系过滤可用。
  - 邮件发送向导可从高校库数据选人、预览、自定义内容、查看发送状态。
- 手动验收：
  - 新建一次采集任务，前端不显示完整爬取日志。
  - 任务完成后能看到最后完成摘要和产出文件。
  - 打开高校库能看到全国本科高校按省市分类，并可筛选 985/211/双一流/普通本科。
  - 已抓取学校能展示教师表格。
  - 选中某校部分教师后能完成测试发送并导出发送日志。

## Assumptions

- Agent 内部日志仍可在后端保留用于排查，但默认不进入前端历史消息。
- 前端模拟进度不需要精确对应 Agent 每一步，只需要表达任务正在进行。
- 985/211 是历史标签，仅用于检索和用户认知。
- v1 不要求补齐每所学校的院系和官网详情；缺失官网的学校先展示名称、省市和标签。
- 邮件系统采用 FastAPI 移植方案，不运行独立 CodexTeac Node 服务。

实施计划

11:19



我会直接开工实现。这里会用 frontend-design 保持两个新入口和表格/发送向导的界面不乱，用 security-review 约束 SMTP 密码、发送确认和日志这几处敏感点。先快速看当前工作树和可复用文件，然后分后端 API、前端入口、任务日志收敛三块改。

已搜索网页 1 次

工作树里已有一些未提交修改，我会只改和这次功能直接相关的文件，并且不回滚现有改动。现在先读 WebSocket/hooks 的现状，确保日志收敛不会踩掉你之前已经做过的调整。

我已经确认当前 WebSocket 前端是把每条 log 直接 append 到消息流，所以“不要刷屏”的改造会集中在后端减少推送/持久化，并让前端把进度更新到同一个占位消息。接下来我会新增后端高校库/邮件模块，再接前端两个对话框。

已搜索网页 1 次

高校名录“全国本科”量很大，手写不现实也容易错；我会把系统做成可加载静态 JSON 名录，并先内置一份覆盖 985/211/双一流与常见普通本科的种子数据，同时接口会把历史抓取里出现的普通本科自动并入。这样现在可用，后续只要替换/补全 JSON 就能扩到教育部完整 1365 所。

教育部页面能直接拿到 2025 名单的 .xls 附件链接，所以我会用它作为生成高校名录的来源，而不是靠记忆硬填。先检查本地有没有读取 .xls 的依赖；如果没有，就把实现做成“种子数据 + 可替换静态 JSON”的安全落点，不把下载脚本强塞进运行链路。

我会先把接口和页面打通，数据名录先采用“内置种子 + 历史自动归档 + 可替换 JSON”的结构。这样不会阻塞功能落地；如果后面要导入教育部完整 1365 所本科名单，只需要替换数据文件，不用再改 API/前端。

现在进入具体修改：我会新增一个高校目录服务、一个邮件服务，把 main.py 接上这些 API；然后前端加两个弹窗式工作区。因为改动面比较大，我会尽量保持模块边界清楚，避免把所有逻辑继续塞进 main.py。

我要补一条数据链路：把教育部 2025 本科名录变成可缓存的数据文件。先把 xlrd 加进本地环境和 requirements，然后我就能用官方 .xls 直接生成高校库 JSON，省掉手工散表。

我已经把本地 xlrd 装上了，下一步可以直接读教育部 .xls 并生成基础名录文件。这样高校库不是凭空造表，而是能从官方名单起步，再叠加你现有任务和标签数据。

我刚发现教育部页面里有两份附件，其中一份是“成人高等学校名单”，不是我们要的普通本科名录。再核一下另一份附件，如果那份才是普通高等学校名单，就直接用它生成基础高校库；如果不是，我就把高校库先做成“静态种子 + 自动归档”结构，不阻塞功能。

太好了，普通高等学校名单就是我们要的官方底座。接下来我会直接把它纳入一个生成脚本，先导出 backend/data/universities.json，再把后端/前端都接到这个数据源上。

我现在会把“高校库”做成真正可查的数据层，而不是只放几个按钮。实现上分两步：先把官方本科名录生成进本地缓存，再把后端 API 和前端工作区接上；同时把 WebSocket 从“日志流”改成“任务摘要流”，让前端只看到进度和结果。

已搜索网页 6 次

我已经把官方数据源确认好了，接下来直接按这个口径落库：本科院校来自教育部 2025 名单，985/211/双一流标签来自现成重点高校列表，前端再按省市做目录展示。这样高校库既是全国底座，也能把你已有抓取结果挂进去。

已搜索网页 10 次

我已经能从教育部本科名单直接读出 2953 行数据，接下来会把 985/211/双一流也做成可解析的官方/权威清单，不靠手抄。这样高校库筛选和标签命中才稳。

已搜索网页 2 次

我已经确认普通本科名录能从教育部官方 .xls 自动生成。现在补标签数据和发送能力，同时把任务日志收口，避免前端继续刷屏。接下来我会尽量把改动压成几个新模块，别继续把 main.py 搞成一锅粥。

已搜索网页 3 次

我已经把官方口径弄清楚了，985 是 39 所、211 是 112 所、第二轮双一流是 147 所。接下来不再犹豫数据来源，直接把它们编码进高校库生成脚本里。

接下来就是真正落代码了。我会新增后端 universities.py、email_service.py，更新 main.py 的接口和 WebSocket 行为，再改前端把高校库/邮件发送入口挂到 header。完成后我会跑类型检查和后端语法检查，确认没有把现有聊天主线弄坏。

已搜索网页 1 次

我已经把官方高校底座拿到了。最后一块是标签：985/211/双一流，我会优先用官方 PDF/页面文本生成名单，而不是手工塞一堆名字。接着开始写文件。

已搜索网页 6 次

我已经把标签口径锁定了：985 官方名单是 39 所，211 官方名单是 112 所，双一流是 147 所。实现里我会把它们都当成标签层，不改变“省市 + 高校”主结构。

已搜索网页 9 次

I’m now pulling the tag layers from a public list page that’s easy to parse, so the UI can filter 985/211/双一流 instead of just showing placeholders. That keeps the implementation moving without inventing a brittle manual spreadsheet.

上下文已自动压缩

我先把现状摸清，再分两条线推进：后端把高校库和邮件能力补上，前端把右上角入口和任务展示收紧。顺手会按 TDD 和安全要求走，免得邮件这块留下隐患。

我在同时看两块：一边是现有任务流和前端消息管线，一边是 CodexTeac 的邮件实现，先把接口和数据流对齐。这里会尽量复用原项目的风格，避免把聊天系统改成另一套陌生的架构。

我已经确认现在的前端会把每条 log 都当成消息塞进列表，这正是要收紧的点。接下来我去把 CodexTeac 的邮件流和这边的后端结构对照一下，顺便找出最适合塞入高校库数据层的位置。

我碰到一个小路径问题，先把技能说明和 CodexTeac 的关键服务文件定位准，再动刀。这样后面改接口和安全边界时，不会靠猜。

我会按这两个技能来推进：tdd-workflow 保证改动可验证，security-review 盯住 SMTP 和用户输入边界。先把规则吃透，再开始写。

已搜索网页 1 次

我现在把页面骨架和状态层摸清楚，准备一次性把 高校库/邮件发送 的打开方式、数据源和任务流都接上。后面会尽量用现有的 Sheet/Dialog 和 store，不另起炉灶。

我已经能看出一个比较稳的落点了：高校库和邮件发送都适合做成后端独立模块，再把前端只当展示层。现在我补一下依赖和现有测试形态，避免一上来就把接口写散。

已搜索网页 3 次

我准备先落一个会失败的测试，把目标钉死：高校筛选、邮件预览/确认、以及“别再把内部日志刷到前端”这三件事。这样后面实现时，不会在细节里跑偏。

我在补一个关键判断：高校库这块是要“真数据可用”，不是只是把几个学校写死。先看看仓库里有没有现成名录或导出的缓存，能省掉不少重复劳动。

已搜索网页 1 次

我现在在确认两件实用事：一是这台环境能不能直接读教育部的 xls，二是要不要把高校名录做成缓存文件而不是每次启动现抓。前者决定实现复杂度，后者决定启动稳定性。

我已经把实现路径定下来了：核心逻辑放后端纯模块，测试也先围绕纯函数和 API 响应做，这样不用先把浏览器拉起来才能验证。接下来我会先写会红的测试，再补实现。

准备开始真正落笔了。我会先补后端测试和模块骨架，再把前端入口和任务展示收口，最后跑一次类型检查和后端冒烟。

我先做一个很小的探路：确认 xlrd 和测试环境都够不够用。要是缺，我就直接补进后端依赖，不让高校库卡在读表这一步。

已搜索网页 1 次

我现在要确认官方本科名单的列结构，这样缓存和筛选字段才能一次写对，不用后面再返工改解析器。

已搜索网页 1 次

我先盯住一条最实在的线：现有输出文件的命名已经带了学校名，这能直接拿来做高校库归档，不必再搞一套复杂的识别器。等数据模块起来，前端就能直接按校查文件、按表格预览了。

我现在会新增后端模块和测试文件，尽量不碰那些历史脚本。测试先用标准库 unittest，因为这个环境没有 pytest，这样验证链路不用额外安装依赖。

已创建 1 个文件

红灯出来了，不过第一下是测试路径没把 backend 放进 sys.path，这不是我们要测的业务红灯。我先把测试入口修正，让失败指向缺失的高校/邮件模块本身。

已编辑 1 个文件

红灯现在已经指向真实缺口：agent.mailer / agent.universities 还不存在。接下来开始补这两个后端模块，先把纯逻辑打通，再把 FastAPI 路由挂上。

我开始补生产代码了：先加 universities.py 负责官方名录/标签/输出文件归档/表格解析，再加 mailer.py 负责 SMTP 会话、预览、发送任务和日志导出。密码只会留在进程内存的会话 map 里。

已创建 2 个文件

后端两个核心模块已写好。现在我把测试跑起来，先修纯逻辑问题，再把它们挂到 main.py 的 API 和 WebSocket 流里。

纯模块基本过了，只有一个测试暴露出我的断言写错：南京邮电大学本身是双一流，不应该落在“普通本科”筛选里。我把测试换成真正普通本科院校，再继续挂 API。

已编辑 1 个文件

纯逻辑已经绿了。接下来改 main.py：新增高校库和邮件 API，同时把 WebSocket 的内部日志从前端消息流里拿掉，只发送少量进度、下载和最终摘要。

已编辑 1 个文件

API 已经挂了一版。现在我改任务流：后台仍然记录摘要级日志到 server logger，但 WebSocket 不再转发 Agent 原始文本；前端只收到 progress、download、done、error。

补丁没命中是因为这个文件里有不少旧编码文本，注释内容和预期不完全一致。我先按行号抓取 WebSocket 段，然后用更稳的上下文替换。

已编辑 1 个文件

这个文件的旧编码导致大块替换继续不太听话，我会用更窄的上下文切进去，只替换实际执行语句，不碰周围历史注释。

已编辑 1 个文件

任务流现在不会再持久化或推送原始 log 了。接下来给前端补类型和 API 方法，然后做两个右上角入口的工作区。

已编辑 2 个文件

前端这边也是旧编码把注释撞歪了，我直接整文件换成干净版本，少跟乱码拉扯。先只做 api.ts，后面再处理页面和工作区组件。

我直接重写 api.ts，顺便把高校库和邮件接口一起加好，这样前端后面接组件会顺很多。

已创建 1 个文件已删除 1 个文件

现在我把 WebSocket 和聊天 hook 的事件形状补齐，接着再做 UI 入口。这样前端会先理解新事件，不会卡在类型上。

这边同样是旧文件版本和我预想有点偏，我直接把整个 websocket.ts 也收拾干净，顺手把 progress 接上。

我已经把消息协议改成能接收 progress 了。现在补前端的两个工作区和任务流收口，让聊天区只剩“进度卡 + 最终摘要 + 下载卡”。

我在收口前端状态层：加两个抽屉入口，把高频内容放进去，聊天主区只留任务结果。顺带会把旧的 log 视图保留成兼容层，但新任务基本不会再出现。

已编辑 2 个文件

我先做一次硬检查，看看后端/前端有没有被旧乱码和接口变动带歪。发现问题我会先修编译，再继续加页面，不让 UI 先跑在假数据上。

我现在补前端状态和页面骨架，尽量把复杂度压在一个地方：ChatArea 头部放入口，Sheet 里承载高校库和邮件向导。聊天消息本体只保留结果，不再刷日志。

已编辑 2 个文件

我再检查一下前端最容易断的地方：任务流 hook、消息渲染、以及页面头部。这里要是处理不稳，UI 会继续刷旧日志。修完这三处就能把工作区塞进去了。

我把后端先稳住了，前端这边我会用“能用优先”的方式补齐，不追求一口气做成完整信息系统，但会把右上角两个入口、筛选、预览、发信流程和任务进度都打通。

我先把类型层和聊天渲染改干净。后面两个工作区会直接吃这些类型，省得接口边改边抖。

已创建 1 个文件已删除 1 个文件

接下来我把 chat-message 改成认识 progress，并把 ChatArea 头部做成真正的入口按钮。这样用户能马上看到两个新功能，而不是藏在别处。

已编辑 1 个文件已删除 1 个文件

我把 ChatArea 重写掉，原因很简单：它是入口层，和旧的日志渲染逻辑绑得太紧。重写后会更容易把右上角入口、任务进度和完成摘要放一起。

已创建 1 个文件

聊天主区现在已经有两个入口了。接下来就是把这两个入口真的做成工作区：左边高校库，右边邮件向导。先做高校库，它的数据和 API 已经就位。

已创建 1 个文件

高校库工作区先落地了。下一步是邮件发送工作区，不过我先要把它做得更像一个向导，别一上来就给用户堆一屏表单。会复用高校库里选中的学校和表格数据。

已创建 1 个文件

工作区骨架已经有了。最后一步是在首页把这两个抽屉挂上去，并确保聊天区和侧边栏还正常。之后我会跑一次 tsc 看具体爆点，再逐个修。

页面文件也带着旧编码，我先把它整文件替掉，保持入口清晰。这个地方很适合干净重写。

已创建 1 个文件已删除 1 个文件

我把入口都挂上了。现在最关键的是类型和编译反馈，我会跑一次 tsc，按错误逐个收，直到前端至少能编过。

PowerShell 还在别扭地拦 &&，我换成原生 Set-Location。这次只要拿到真实的 TypeScript 报错就行。

很好，这是一组很干净的编译错误，说明功能线已经接起来了，只差把类型和旧分支收紧。现在我会补几个小型类型适配，不再碰大结构。

已编辑 2 个文件

use-task-stream.ts 也被旧文本污染了，我先整文件重写成干净版，避免继续在乱码里打补丁。这个文件本来就该薄一些。

已创建 1 个文件已删除 1 个文件

use-task-stream 已经干净了。现在再处理 use-agent-chat 里那些 unknown 和对话更新的类型噪音，然后就能重新编译。

我继续把前端工具层压平，尽量让编译错误只剩真正值得看的地方。现在再跑一次 tsc，看还剩哪些收尾项。

还剩下几个小钉子：工作区里 unknown 返回值、use-agent-chat 的一个旧 task_id 解构、还有 websocket.ts 里已经不该再有的 log 分支。都是收尾活，我会一口气修掉。

我已经看到 websocket.ts 还残留了旧的 log 分支，而后端新协议已经不再发它。顺手把这个文件重写成只认新事件，类型问题就会安静很多。

已创建 1 个文件已删除 1 个文件

我继续把 API 返回值标出来，这样工作区和聊天流就不会再把 unknown 当空气来用。收完这几个点，再跑编译。

我先看一眼 api.ts 有没有被我写坏，再处理工作区里剩下的 unknown。这种活儿很容易在一个小拼写里藏雷。

我已经把编译错误缩小到很明确的几处了。现在就是给工作区和 use-agent-chat 补上返回值类型，剩下的就能顺着过去。

我把收尾重点放在两处：一是给 API 方法补足返回类型，二是把工作区里那些 unknown 结果收掉。这样不用再靠 hook 里硬转。

已编辑 2 个文件

已编辑 17 个文件+1,903-656撤销审核

backend/tests/test_feature_contracts.py

backend/tests/test_feature_contracts.py

backend/agent/universities.py

再显示 14 个文件

