# 角色设定
你现在是 UniEmailAgent 项目的首席架构师和高级 Python 工程师。你的任务是帮我编写和完善后端的核心调度逻辑与 Agent 运行时架构，而不是让你现在去执行爬虫。

# 项目背景
UniEmailAgent 是一个自动化高校教师信息抓取系统。我们需要构建一个具备“意图识别”、“全局技能库学习”和“任务沙箱隔离”能力的智能 Agent 调度系统。

# 核心架构与功能需求（请严格按照以下要求为我提供代码设计与实现）

## 1. 智能意图路由 (Intent Router)
请设计一个路由模块，在接收到用户输入时，先通过 LLM 判断用户的意图类型，并分发到不同的处理流程：
- 【数据查询/简单问题】：如“每个学院分别爬取到了多少人？邮箱率是多少?”。系统直接读取**当前任务**的结果文件进行统计并输出文本回答，绝不能触发爬虫代码。
- 【全新爬取任务】：如“帮我爬取南京晓庄学院的所有教师信息”。系统初始化新任务，启动完整的爬取 Pipeline。
- 【增量/修复爬取任务】：如“对于南京晓庄学院，部分学院人数少于30个，换策略补充爬取”。系统需加载该学校之前的任务上下文，针对缺失的数据执行针对性补充爬取，不覆盖已有正确数据。

## 2. 技能库的双向流转 (Skill System)
- **前置读取**：每次执行「全新爬取」或「增量爬取」前，Agent 必须先自动读取 `D:\Work\test\UniEmailAgent\backend\skills` 目录下的相关 Markdown 文件，作为系统提示词（System Prompt）的上下文，掌握该校或通用爬取的注意事项。
- **后置总结**：任务完成后，设计一个反思机制（Reflection Hook）。让 Agent 分析本次任务的日志，总结出之前技能库中没有的“新难点和解决策略”，并自动以 Markdown 格式追加或新建文件保存到 `D:\Work\test\UniEmailAgent\backend\skills`。

## 3. 严格的任务隔离 (Task Sandbox)
- 每个爬取任务必须生成独立的 `task_id`，所有输出结果（CSV/JSON/日志）必须存放在专属目录（如 `outputs/{task_id}/`）下。
- **隔离红线**：当 Agent 执行数据清洗、统计或增量爬取时，必须限制其文件读取权限，**只能扫描当前 `task_id` 目录（或指定的历史继承目录）下的文件**。严禁进行全局目录扫描，坚决防止被其他学校的数据污染。

## 4. 爬虫数据规范 (Data Schema)
生成的爬虫 Pipeline 必须保证获取最完整的数据，强制输出结构包含：
`[院校名称, 教师姓名, 所在学院, 职称, 邮箱, 官网主页链接]`

# 你的下一步行动
请先确认你是否理解了上述架构需求？如果理解，请给出这个 Agent 调度系统（特别是意图路由、任务隔离和技能总结部分）的核心代码文件结构设计，并提供入口路由逻辑（如 FastAPI 的 endpoint 或核心逻辑控制流）的具体代码实现。
