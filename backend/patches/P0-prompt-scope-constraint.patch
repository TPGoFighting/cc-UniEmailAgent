# P0-3: Agent 不遵守范围约束
## 问题
当用户指定特定学院（如"计算机科学与技术学院"）时，Agent 仍然爬取了全校 16 个学院。

## 根因
`intent_router.py` 的 `IntentResult.target_departments` 已提取学院名称，但 `main.py` 构建 prompt 时完全忽略了该字段。Agent 只看到用户原始消息，没有被明确告知"只爬这个学院"。

## 修复方案
在 `main.py` 的 prompt 注入部分（约 L1540-1561），当 `intent_result.target_departments` 非空时，追加一条范围约束指令。

代码位置：`backend/main.py`，`# 注入到 prompt 前面` 注释之后

注入内容示例：
```
## 🔒 爬取范围约束
用户指定了以下学院作为范围：
- 计算机科学与技术学院

注意：
1. 只爬取以上指定学院的教师数据，不要爬取其他学院
2. 如果找不到完全匹配的学院名称，查找最接近的学院（如"信息科学技术学院"可能包含计算机类专业）
3. 产出文件只包含指定学院的数据
```
